"""Address dependencies for dependency injection."""

# This module can be extended with custom dependencies if needed.
# For now, addresses uses the standard auth dependencies from src.auth.dependencies
